<!DOCTYPE html>
<html lang="da">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mine Adoptioner</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-color: #f4f6f9;
            font-family: Arial, sans-serif;
        }
        .sidebar {
            height: 100%;
            background-color: #2c3e50;
            color: white;
            padding-top: 20px;
            position: fixed;
            width: 250px;
            left: 0;
        }
        .sidebar a {
            color: white;
            padding: 12px 20px;
            text-decoration: none;
            display: block;
        }
        .sidebar a:hover {
            background-color: #1abc9c;
        }
        .content {
            margin-left: 260px;
            padding: 20px;
        }
        .page-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .animal-card {
            text-align: center;
            background-color: white;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .animal-card img {
            max-width: 100%;
            border-radius: 10px;
        }
        .animal-card h3 {
            margin-top: 10px;
            color: #2c3e50;
        }
        .animal-card p {
            font-size: 1rem;
        }
        .no-adoptions {
            text-align: center;
            margin-top: 50px;
            font-size: 1.2rem;
            color: #555;
        }
    </style>
</head>
<body>
<!-- Sidebar -->
<div class="sidebar">
    <h3 class="text-center">Dyreinternat Dashboard</h3>
    <a href="/dashboard">Hjem</a>
    <a href="/adoption">Adoption</a>
    <a href="/myadoptions" class="active">Mine adoptioner</a>
</div>

<!-- Content -->
<div class="content">
    <h2 class="page-header">Mine Adoptioner, <%= username %>!</h2>
    <div class="row">
        <% if (userAdoptions.length > 0) { %>
            <% userAdoptions.forEach(animal => { %>
                <div class="col-md-4 mb-4">
                    <div class="animal-card">
                        <img src="<%= animal.image ? animal.image : '/images/default-animal.jpg' %>" alt="<%= animal.name %>">
                        <h3><%= animal.name %></h3>
                        <p><%= animal.description %></p>
                    </div>
                </div>
            <% }); %>
        <% } else { %>
            <div class="no-adoptions">
                <p>Du har ingen adoptioner endnu. Tag et kig på vores <a href="/adoption">adoptionsside</a>.</p>
            </div>
        <% } %>
    </div>
</div>
</body>
</html>
